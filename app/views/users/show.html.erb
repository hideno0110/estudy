<aside class="sidebar">
  <section>
    <span class="label">ユーザー</span>
    <h3 class="user-info"><%= @user.name %></h3>
  </section>
  
  <section>
    <span class="label">メールアドレス</span>
    <h3 class="user-info"><%= @user.email %></h3>
  </section>

  <section>
    <small><%= link_to 'プロフィールを編集', edit_user_path(@user) %></small>
  </section>
  <%= link_to @user.likes.count, like_articles_user_path(@user.id) %>個の投稿にいいね!しています
</aside>

<ul class="notes">
  <h2><%= @title %></h2>
  <%= render @articles %>
</ul>
